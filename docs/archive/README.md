Earlier drafts, kept for history. None of them is current.

- `YOLO_SETUP.md`: first draft; a `yolo` user and a reusable template image.
- `ATLAS.md` with `yolo-dev.yaml` and `network-acl.yaml`: second draft; the cloud image provisioned by cloud-init.
- `setup-vm.sh`: third draft; the desktop image with a one-shot setup script, replaced by `guest/yolovm-guest`.
